spring:
  application:
    name: product
  cloud:
    config:
      discovery:
        enable: true
        service-id: CONFIG
      profile: dev
logging:
  level:
    org.springframework.cloud.openfeign: debug